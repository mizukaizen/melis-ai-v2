---
title: "Set up the test so you can trust the result"
eyebrow: "Prompt Library · Data"
lede: "An A/B test design with hypothesis, sample size, success criteria, and guardrail metrics — so you learn something real instead of running an experiment you can argue either way."
category: "Data"
categorySlug: "data"
categoryGroup: "Build"
subcategory: "test"
payoff: "save"
icon: "ph-chart-scatter"
tier: "pro"
sourceId: "da-05"
cover: "/section-rooms/prompts-data.png"
prompt: "You are a Python data science expert specialising in pandas, data wrangling, and automated analysis pipelines. I want to build a data processing pipeline in Python.\n\nInput data: [DESCRIBE THE SOURCE — CSV / JSON / DATABASE / API; STRUCTURE AND SIZE]\nProcessing requirements: [WHAT TRANSFORMATIONS ARE NEEDED — CLEAN / MERGE / AGGREGATE / RESHAPE / ENRICH]\nOutput format: [WHAT THE PIPELINE MUST PRODUCE — AND WHERE IT GOES]\nFrequency: [ONE-OFF SCRIPT / DAILY BATCH / REAL-TIME STREAM]\nScale: [ROWS TO PROCESS — AFFECTS PANDAS VS. POLARS VS. SPARK CHOICE]\n\nPlease produce:\n1. Pipeline architecture — the stages, data shapes at each stage, and error handling strategy\n2. Library recommendation — pandas vs. polars vs. dask vs. pyspark for this scale; rationale\n3. Complete pipeline code — production-quality Python with: type hints, logging, validation at each stage, error handling\n4. Data validation layer — the checks to run on input data before processing; what to do on failures\n5. Testing approach — how to test each transformation in isolation; sample test data strategy\n6. Performance profiling — how to measure where the pipeline spends its time; optimisation targets\n7. Scheduling and orchestration — how to run this on a schedule; Cron vs. Airflow vs. Prefect for this use case\n\nData pipelines are trust systems. Validate everything."
draft: false
---

An A/B test design with hypothesis, sample size, success criteria, and guardrail metrics — so you learn something real instead of running an experiment you can argue either way.
