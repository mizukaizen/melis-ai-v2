---
title: "Find exactly why a prompt isn't working — and fix it"
eyebrow: "Prompt Library · Prompting"
lede: "A systematic prompt debug process that identifies whether the failure is in the instruction, the context, the format, or the model's limitations — and fixes the right thing."
category: "Prompting"
categorySlug: "prompting"
categoryGroup: "Build"
subcategory: "optimise"
payoff: "save"
icon: "ph-command"
tier: "free"
sourceId: "pe-01"
cover: "/section-rooms/prompts-prompting.png"
prompt: "You are a world-class prompt engineer who has studied every major LLM system and developed prompts across GPT-4, Claude, Gemini, Llama, and Mistral. I want to understand how to write prompts that reliably produce excellent results.\n\nMy use case: [DESCRIBE WHAT YOU'RE TRYING TO DO WITH LLMs — CONTENT GENERATION / ANALYSIS / CODE / EXTRACTION / CLASSIFICATION / CONVERSATION]\nModels I'm using: [GPT-4 / CLAUDE / GEMINI / LLAMA / MISTRAL / MIXTRAL — SPECIFY]\nCurrent prompt: [PASTE YOUR CURRENT PROMPT — OR DESCRIBE WHAT YOU'VE TRIED]\nThe problem: [WHAT'S WRONG WITH CURRENT OUTPUTS — TOO VAGUE / WRONG FORMAT / INCONSISTENT / MISSING KEY CONTENT]\n\nPlease produce:\n1. Prompt diagnosis — what is structurally wrong with the current prompt; the root causes of poor output\n2. Prompt rewrite — a dramatically improved version of the prompt using best practices\n3. Annotated explanation — explain each change made and why it produces better results\n4. Prompt design principles — the 8 universal principles that apply to every high-quality prompt\n5. Format control techniques — how to reliably get structured output (JSON, markdown, lists, tables) from this model\n6. Testing protocol — how to systematically test whether a prompt is reliable; the test cases to run\n7. The one thing that would most improve this prompt immediately\n\nGood prompts are good specifications. Write them with the same rigour."
draft: false
---

A systematic prompt debug process that identifies whether the failure is in the instruction, the context, the format, or the model's limitations — and fixes the right thing.
