---
title: "Break your own prompt before someone else does"
eyebrow: "Prompt Library · Prompting"
lede: "Adversarial prompt testing that checks for jailbreaks, edge cases, and the inputs that produce outputs you didn't expect — before you ship it."
category: "Prompting"
categorySlug: "prompting"
categoryGroup: "Build"
subcategory: "optimise"
payoff: "save"
icon: "ph-dna"
tier: "pro"
sourceId: "pe-14"
cover: "/section-rooms/prompts-prompting.png"
prompt: "You are an expert in prompt compression, token efficiency, and optimising LLM costs without sacrificing quality. I want to reduce the token cost of my prompts while maintaining output quality.\n\nCurrent prompt:\n```\n[PASTE YOUR CURRENT PROMPT]\n```\nCurrent token count: [APPROXIMATE — USE TOKENISER OR ESTIMATE]\nQuality that must be preserved: [THE ASPECTS OF OUTPUT QUALITY THAT CANNOT DEGRADE]\nCost target: [% REDUCTION IN TOKENS DESIRED — OR MONTHLY COST TARGET]\nVolume: [HOW MANY TIMES THIS PROMPT RUNS PER DAY / MONTH]\n\nPlease produce:\n1. Token audit — analyse the current prompt; categorise every section as: essential / helpful / redundant / harmful\n2. Compression techniques — the specific methods applicable here: implicit instructions, abbreviation, structured formats, removing redundancy\n3. Compressed prompt — the reduced version, with token count estimate; what was cut and why each cut is safe\n4. Quality regression test — the 5 test cases to run to confirm quality hasn't degraded\n5. Dynamic prompt techniques — how to make parts of the prompt conditional; only include sections when needed\n6. Caching strategy — what parts of the prompt are static and could benefit from prompt caching (if your API supports it)\n7. Cost model — the monthly savings from the compression at your stated volume\n\nToken efficiency is cost efficiency. Measure before and after."
draft: false
---

Adversarial prompt testing that checks for jailbreaks, edge cases, and the inputs that produce outputs you didn't expect — before you ship it.
